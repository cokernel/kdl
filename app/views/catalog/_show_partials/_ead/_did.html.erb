<h5>
  <% if element.did -%>
    <%= fulltitle(element.did) %>
  <% end -%>
</h5>

<span class="physdesc"><%= element.did.physdesc %></span>

<% if !element.did.containers.blank? -%>
  <% containers = [] -%>
  <span class="container">
    <% element.did.containers.each_with_index do |container, index| -%>
      <% containers << [container_type(element.did, index), container].join(': ') %>
    <% end -%>
    [<%= containers.join(', ') %>]
  </span>
<% end -%>

