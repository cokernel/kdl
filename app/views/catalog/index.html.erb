<% unless has_search_parameters? %>
	<%# if there are no input/search related params, display the "home" partial -%>
	<%= render 'home' %>
<% else %>

	<% @page_title = application_name + " Search Results" %>

	<%= render :partial => 'search_form', :locals => {:extra_info => @extra_info} %>
  <%= render :partial => 'collections_browse' %>
	
	<%= render :partial => 'did_you_mean' %>

	<%= render 'constraints', :localized_params=>params %>

  <%#= render :partial => 'browse' %>

  <%= render 'catalog/top_sidebar' %>

	<%= render 'sort_and_per_page' %>
  
  <%= render 'results_pagination' %>
  

	<%= render_document_list_partial %>

	<% sidebar_items << capture do %>
	  <%= render :partial=>'facets' %>
	<% end %>

  <%= render 'results_pagination' %>


	<%= render :partial=>'footer' %>

<% end %>

